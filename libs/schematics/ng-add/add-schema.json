{
  "$schema": "http://json-schema.org/schema",
  "id": "ngx-metaui-rules-ng-add",
  "title": "Angular Service Worker Options Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "skipDependencies": {
      "type": "boolean",
      "description": "Tells if we should install required package.json dependencies",
      "default": false,
      "x-prompt": "Do not add required dependencies to package.json (e.g., --skipDependencies)?"
    },
    "skipNpmInstall": {
      "type": "boolean",
      "description": "Tells if we should skip npm install tasks",
      "default": false,
      "x-prompt": "Do not run 'npm install' (e.g., --skipNpmInstall)?"
    },
    "skipStyles": {
      "type": "boolean",
      "description": "Don't add anything to angular.json style section.",
      "default": false,
      "x-prompt": "Do you want to update styles in angular.json (e.g., --skipStyles)?"
    },
    "skipScripts": {
      "type": "boolean",
      "description": "Don't add anything to angular.json script section",
      "default": false,
      "x-prompt": "Do you want to update scripts in angular.json (e.g., --skipScripts)?"
    },
    "path": {
      "type": "string",
      "description": "Root path to the selected project  - a place where template files will be copied"
    }
  },
  "required": []
}
